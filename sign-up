<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <meta name="viewport" content="width=device-width, initial-scale=1.0">
  <title>Sign Up – JUSTBUY</title>
  <link href="https://fonts.googleapis.com/css2?family=Montserrat:wght@700;400&display=swap" rel="stylesheet">
  <style>
    body {
      background: linear-gradient(135deg, #3b82f6 0%, #8b5cf6 100%);
      min-height: 100vh;
      margin: 0;
      font-family: 'Montserrat', Arial, sans-serif;
      display: flex;
      align-items: center;
      justify-content: center;
    }
    .signup-container {
      background: #fff;
      border-radius: 18px;
      box-shadow: 0 8px 32px rgba(59,130,246,0.18);
      padding: 48px 36px 36px 36px;
      max-width: 400px;
      width: 100%;
      text-align: center;
      position: relative;
      overflow: hidden;
    }
    .signup-container::before {
      content: '';
      position: absolute;
      top: -60px;
      left: -60px;
      width: 180px;
      height: 180px;
      background: linear-gradient(135deg, #8b5cf6 0%, #3b82f6 100%);
      border-radius: 50%;
      opacity: 0.12;
      z-index: 0;
    }
    .logo {
      margin-bottom: 18px;
    }
    .logo img {
      width: 70px;
      border-radius: 12px;
      box-shadow: 0 2px 8px rgba(59,130,246,0.12);
    }
    h2 {
      color: #3b82f6;
      font-size: 2.1rem;
      margin-bottom: 10px;
      font-weight: 700;
      letter-spacing: -1px;
    }
    p {
      color: #6b7280;
      margin-bottom: 28px;
      font-size: 1.05rem;
    }
    form {
      display: flex;
      flex-direction: column;
      gap: 18px;
      z-index: 1;
      position: relative;
    }
    input[type="email"],
    input[type="password"],
    input[type="text"] {
      padding: 13px 16px;
      border-radius: 8px;
      border: 1.5px solid #d1d5db;
      font-size: 1rem;
      background: #f3f4f6;
      transition: border 0.2s;
      font-family: inherit;
    }
    input:focus {
      border: 1.5px solid #3b82f6;
      outline: none;
      background: #fff;
    }
    .btn-signup {
      background: linear-gradient(90deg, #3b82f6 0%, #8b5cf6 100%);
      color: #fff;
      border: none;
      border-radius: 8px;
      padding: 13px 0;
      font-size: 1.1rem;
      font-weight: 700;
      cursor: pointer;
      box-shadow: 0 4px 16px rgba(59,130,246,0.13);
      margin-top: 8px;
      transition: background 0.2s, transform 0.2s;
    }
    .btn-signup:hover {
      background: linear-gradient(90deg, #2563eb 0%, #8b5cf6 100%);
      transform: translateY(-2px) scale(1.03);
    }
    .login-link {
      margin-top: 18px;
      color: #6b7280;
      font-size: 0.98rem;
    }
    .login-link a {
      color: #3b82f6;
      text-decoration: none;
      font-weight: 600;
      transition: color 0.2s;
    }
    .login-link a:hover {
      color: #8b5cf6;
      text-decoration: underline;
    }
    .error-message {
      color: #ef4444;
      font-size: 0.98rem;
      margin-bottom: 8px;
      display: none;
    }
    @media (max-width: 500px) {
      .signup-container {
        padding: 32px 10px 24px 10px;
      }
      h2 {
        font-size: 1.5rem;
      }
    }
  </style>
</head>
<body>
  <div class="signup-container">
    <div class="logo">
      <img src="https://i.imgur.com/sLpob3a.png" alt="JUSTBUY Logo">
    </div>
    <h2>Create Your Account</h2>
    <p>Sign up to start buying and selling on JUSTBUY!</p>
    <div class="error-message" id="error-message"></div>
    <form id="signup-form" autocomplete="off">
      <input type="text" id="signup-username" placeholder="Username" required>
      <input type="email" id="signup-email" placeholder="Email" required>
      <input type="password" id="signup-password" placeholder="Password" required minlength="6">
      <button type="submit" class="btn-signup">Sign Up</button>
    </form>
    <div class="login-link">
      Already have an account? <a href="login.html">Log in</a>
    </div>
  </div>
  <script>
    const signupForm = document.getElementById('signup-form');
    const errorMessage = document.getElementById('error-message');
    
    signupForm.addEventListener('submit', function(e) {
      e.preventDefault();
      const email = document.getElementById('signup-email').value.trim();
      const password = document.getElementById('signup-password').value;
      const username = document.getElementById('signup-username').value.trim();
      
      errorMessage.style.display = 'none';
      
      if (!username) {
        errorMessage.textContent = 'Please enter a username.';
        errorMessage.style.display = 'block';
        return;
      }
      
      if (!email) {
        errorMessage.textContent = 'Please enter an email address.';
        errorMessage.style.display = 'block';
        return;
      }
      
      if (password.length < 6) {
        errorMessage.textContent = 'Password should be at least 6 characters.';
        errorMessage.style.display = 'block';
        return;
      }
      
      // Check if user already exists
      const existingUsers = JSON.parse(localStorage.getItem('justbuy_users') || '[]');
      const userExists = existingUsers.find(user => user.email === email);
      
      if (userExists) {
        errorMessage.textContent = 'Email is already in use.';
        errorMessage.style.display = 'block';
        return;
      }
      
      // Create new user
      const newUser = { email, password, username };
      existingUsers.push(newUser);
      localStorage.setItem('justbuy_users', JSON.stringify(existingUsers));
      
      // Save current user session
      sessionStorage.setItem('currentUser', JSON.stringify({ email, username }));
      
      window.location.href = 'index.html';
    });
  </script>
</body>
</html>
